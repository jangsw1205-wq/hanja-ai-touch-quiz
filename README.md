# AI 한자 터치 퀴즈

실제 OpenAI API를 서버에서 호출해, 사용자가 입력한 한자 목록 안에서 모양·음·훈이 비슷한 오답 3개를 골라주는 앱입니다.

## 실행
1. Node.js 설치
2. 이 폴더에서 `npm install`
3. `OPENAI_API_KEY` 환경변수에 본인 API 키 설정
4. `npm start`
5. 브라우저에서 `http://localhost:3000`

API 키를 HTML 파일에 넣지 마세요. 서버 환경변수로만 보관합니다.
